syntax = 'proto3';

message Sound {
    uint32 x = 1;
    uint32 y = 2;
    enum SoundType {
        // TODO find out all sound types and match their enum to their constant value
        UNKNOWN = 0;
        
    }
    SoundType block_type = 3;
}